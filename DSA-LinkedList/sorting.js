//when item1 < item 2, change pointer so that next > previous
//but what is the best way do do this sort! want most efficient

function sort(list){
    let curr = list.head;
    let next = list.head.next;
}